// Example script for handling interactions (if needed)
document.querySelectorAll('.action-btn').forEach(button => {
    button.addEventListener('click', () => {
      alert('Вы добавили товар в корзину!');
    });
  });